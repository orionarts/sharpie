import { VerticalBox } from "std-widgets.slint";

// Engines tab {{{1
//
export component EnginesTab inherits VerticalBox {
    callback field-edited();

    Text {
        text: "Engines (TODO)";
    }
}
