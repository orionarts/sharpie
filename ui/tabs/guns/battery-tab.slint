import { VerticalBox } from "std-widgets.slint";

// Battery tab {{{1
//
// Once instance for each battery
//
export component BatteryTab inherits VerticalBox {
    callback field-edited();

    Text {
        text: "Battery (TODO)";
    }
}
