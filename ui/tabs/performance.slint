import { VerticalBox } from "std-widgets.slint";

// Performance tab {{{1
//
export component PerformanceTab inherits VerticalBox {
    callback field-edited();

    Text {
        text: "Performance (TODO)";
    }
}
