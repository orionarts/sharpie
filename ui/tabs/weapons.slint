import { VerticalBox } from "std-widgets.slint";

// Weapons tab {{{1
//
export component WeaponsTab inherits VerticalBox {
    callback field-edited();

    Text {
        text: "Weapons (TODO)";
    }
}
