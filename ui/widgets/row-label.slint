import { Layout }        from "../layout.slint";

/// Right-justified label for row headers
///
export component RowLabel inherits Text {
    height: Layout.edit-height;
    vertical-alignment: center;
    horizontal-alignment: right;
}
